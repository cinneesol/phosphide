{
  "private": true,
  "name": "phosphide-example",
  "dependencies": {
    "codemirror": "^5.9.0",
    "phosphide": "file:.."
  },
  "devDependencies": {
  },
  "scripts": {
    "build": "tsc --project blue && tsc --project green && tsc --project red && tsc --project yellow && tsc --project editor && webpack --config webpack.conf.js",
    "postinstall": "npm dedupe"
  }
}
